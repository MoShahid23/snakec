#include "food.h"

void food_init(Food *food, const char *name, int x, int y, int value, GameConfig *config) {
    //initialize food with arguments
}

bool food_eaten(Food *food, Snake *snake){
    //check if snake and food positions match, return true
}